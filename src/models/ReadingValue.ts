
/// An intermediate value used to pass information from the readings screen -> treatments screen
export interface ReadingValue {
    // The reading's var
    var: string;
    // The value associated with the reading
    value: number;
}
